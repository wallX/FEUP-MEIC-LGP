services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - tusd
  tusd:
    image: tusproject/tusd
    ports:
      - "1080:8080"
    volumes:
      - ./uploads:/srv/tusd-data/data